user root;
worker_processes 1;
events {
}

http {
    server {
        listen       80;
        server_name  77.222.54.5;

        location / {
            proxy_pass http://frontend:80/;
        }

        location /api {
            proxy_pass http://backend:8000/;
        }
    }
}
